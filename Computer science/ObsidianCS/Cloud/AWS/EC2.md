What means EC2?

EC2 = Elastic Compute Cloud

In [[AWS]] the EC2 service is the [[Virtual Machines]] in the [[Cloud]] which are [[Instances]] that we can use to run the different scripts, [[ETL]] or [[Process]]

This services is [[serverless]], for that reason the name **"ELASTIC"** because we can change the size according with the demand. More [[RAM Memory]], [[CPU]], erc ..