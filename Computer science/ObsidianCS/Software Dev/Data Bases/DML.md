## What is the DML?

Known as **Data Manipulation Language** is an internal language used in [[SQL]] to manipulate the [[DataBase]]. The main commands are **SELECT**, **INSERT**, **UPDATE**, **DELETE**
